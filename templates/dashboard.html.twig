{% extends 'base.html.twig' %}
{% block body %}
{{ parent() }}
<!-- Content -->
    <h1>Dashboard</h1>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mt-3">
            {{ message }}
        </div>
    {% endfor %}
{% endblock %}